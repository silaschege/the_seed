import 'package:flutter/material.dart';

class SplashScreenGreen extends StatelessWidget {
  const SplashScreenGreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
