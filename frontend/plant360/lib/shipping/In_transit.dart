import 'package:flutter/material.dart';

class InTransitPage extends StatelessWidget {
  const InTransitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("InTransitPage"),
    );
  }
}
