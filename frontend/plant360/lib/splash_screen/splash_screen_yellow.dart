import 'package:flutter/material.dart';

class SplashScreenYellow extends StatelessWidget {
  const SplashScreenYellow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
